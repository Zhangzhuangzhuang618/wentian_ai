# WT-087 深度自审

> 日期：2026-08-29
> 结论：代码、迁移、部署、合成验证和真实页面核心结构核验通过；扩展端回传和自动刷新待 Chrome 烟测

## Surface 与数据隔离

- `deepseek_web`使用独立 Surface、产品标签、Origin、适配器版本和页面签名；
- 创建运行、任务预检、当前页面和采集草稿均绑定同一 Surface；
- DeepSeek 证据不能写入豆包或千问运行，既有两类平台数据没有迁移或改写；
- 数据库新增一条`active`配置，不改变既有 Surface 记录。

## 采集与自动化边界

- 扩展只允许`https://chat.deepseek.com`根路径和`/a/chat`会话路径，明确拒绝`/sign_in`；
- 首版只记录回答区域内真实可见 HTTP(S) 链接、回答正文和当前视口截图；
- 不读取 Cookie、Web Storage、隐藏网络响应、账号凭证，不处理登录、验证码或滑块；
- 回答只显示媒体名或纯文本域名时不生成正式引用；
- DeepSeek 使用独立部署变量，自动化默认`production + none`并失败关闭；
- 合成环境可验证页面驱动，所有自动结果仍进入人工复核。

## 发现与处理

| 编号      | 风险                                                          | 处理                                                      | 结果   |
| --------- | ------------------------------------------------------------- | --------------------------------------------------------- | ------ |
| SR-087-01 | 仅增加前端选项会把 DeepSeek 证据错记为现有 Surface            | 同步扩展契约、适配器、迁移、运行预检和浏览器页面校验      | 已处理 |
| SR-087-02 | 页面白名单查找回调引用尚未初始化的`entry`，三个平台均无法注入 | 改为在查找回调参数中直接取得`surfaceCode`并增加全平台回归 | 已处理 |
| SR-087-03 | 把登录页当作聊天页会导致整批流程错误启动                      | `/sign_in`失败关闭并增加路径测试                          | 已处理 |
| SR-087-04 | 没有真实链接的回答被推断为有信源                              | 只接受回答区域内可验证 HTTP(S) 链接                       | 已处理 |
| SR-087-05 | DeepSeek 误用豆包或千问生产授权配置                           | 增加独立环境变量、运行时加载器和预检测试                  | 已处理 |
| SR-087-06 | 合成测试被误写为正式页面可用                                  | 任务和自审明确保留真实页面烟测项                          | 已处理 |
| SR-087-07 | 真实页面使用“开启新对话”，首版规则只接受“开启对话”            | 扩展`0.0.23`补充真实标签并把页面签名升级为`@2`            | 已处理 |

## 验证证据

- `pnpm verify`通过：433项测试，0项失败；
- TypeScript、源码边界、OpenAPI一致性和格式检查通过；
- 独立部署基础检查通过，共11个连续迁移；
- `0011_deepseek_web_surface.sql`已实际执行；
- 数据库记录为`deepseek_web | DeepSeek 网页版 | deepseek-web@1-visible-page | active`；
- `GET /health/live`返回`ok`；
- `GET /health/ready`返回`ready`，数据库和对象存储均为`ready`；
- 部署后的首页资源包含`deepseek_web`和“DeepSeek Web”运行选项。
- 已登录真实页面确认根路径编辑器为`textarea[name="search"]`，占位文本为“给 DeepSeek 发送消息”；
- 经用户确认发送测试问题后，页面进入`/a/chat/s/{id}`并完成回答；页面显示“已阅读 12 个网页”；
- 回答正文容器包含13个可点击 HTTP(S) 链接条目，按完整URL精确去重后为8条；没有从纯文本名称推断链接；
- 真实页面新对话入口为“开启新对话”，已补充识别并增加回归断言。

## 未完成项

- 内置浏览器没有加载本地问天扩展，因此尚未完成扩展内容脚本到问天API的真实回传和工作台自动刷新；
- 需在 Chrome 重新加载`0.0.23`后完成一次单题或整批端到端烟测；
- DeepSeek 独立信源面板如需支持，必须基于真实页面可见结构另立任务，不能用首版通用链接采集结果代替。

## 结论

WT-087 的系统接入、部署、失败关闭边界和 DeepSeek 真实页面核心结构已经通过自审。当前仍不能把浏览器页面核验表述为扩展端到端回传完成；在 Chrome 完成`0.0.23`回传和问天自动刷新前，任务保持未完全关闭。
