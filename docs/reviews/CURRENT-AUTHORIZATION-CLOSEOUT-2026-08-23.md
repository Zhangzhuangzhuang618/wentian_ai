# 当前授权范围收尾审计

> 历史记录：本审计形成于WT-054～WT-059获批和落地之前。当前状态以[`WT054-WT059深度自审`](./WT054-WT059-DEEP-SELF-REVIEW-2026-08-23.md)为准。

> 日期：2026-08-23  
> 审计范围：消费端观察优先开发、消费端信源自述及不越过现有产品/合规/生产门禁的后续工作  
> 结论：当前可执行的无外部调用、无生产启用工作已完成；以下事项需要项目所有者或专项评审提供新输入

## 已完成范围

- 独立项目、scope和不可变问题集快照基础；
- 消费端观察领域、草案契约、用户在场采集原型和合成HTTP闭环；
- 运行、任务、一次性采集凭据、待复核证据、确认/拒绝事务和不可变`cited`事件的内存实现；
- 消费端观察指标、运行级批次、按需查询、严格DTO和合成内部E2E；
- 消费端信源自述的确定性显式域名提取、人工复核、不可变`nominated`事件、运行级指标和严格DTO；
- 不可核验内部抓取统计声明的版本化证据警告，且不传播回答自报数值；
- 提名—引用重合度的已批准固定K纯计算和严格DTO。

## 必须补充的产品与指标决策

1. 版本化信源自述提示词最终文本、版本责任人和变更规则；
2. 同题多样本如何形成提名与引用排行榜，包括主排序指标和并列项规则；
3. “相近时间”的运行时间窗长度、使用创建/开始/完成时间中的哪一类时间，以及政策版本；
4. 跨问题是否聚合提名—引用重合度；如聚合，采用问题等权还是样本加权；
5. WT-004列出的首发Provider、真实model ID、方法版本、样本与预算、保留字段/期限和条款责任人；
6. WT-005列出的豆包Surface条款结论、允许地区、截图/回答/DOM保留期、删除行为和责任人。

在第2和第3项未落档前，不得把当前按域名字母排序的指标数组当排行榜，也不得生成运行级`nomination-comparisons`数值。

## 必须补充的生产与数据授权

- 数据库正式迁移序号、生产事务、Outbox和对象存储策略；
- 本地身份初始化、生产会话、CSRF、限流、审计和密钥策略；
- 正式消费端路由、Surface激活和真实观察包上传；
- Provider凭证和受控真实灰度；
- 数据保留、删除和备份恢复政策；
- 自有域名验证方式、爬虫身份快照来源和日志导入授权；
- 独立部署环境与发布责任人。

## 后续阶段门禁

- Web、Worker和生产API依赖正式迁移及身份/保留策略，当前不以静态假页面或无持久化接口冒充完成；
- API—消费端数值重合度的公式仍未批准，只保留既有缺口记录；
- GEO连接器属于独立系统发布后的阶段G，不在当前消费端优先范围内提前实现；
- Git仓库继续等待项目所有者创建，不执行`git init`。

## 继续开发的触发条件

项目所有者只需先补齐以下任一组输入，即可签发下一批任务：

- 指标组：多样本排名规则 + 相近时间窗规则；
- Surface组：豆包条款/地区 + 三类证据保留期 + 责任人；
- Provider组：首发Provider/model + 预算 + 白名单字段/保留期 + 条款复核；
- 生产基础组：迁移授权 + 身份/会话策略 + 对象存储/删除策略；
- Git组：已创建仓库及分支/提交约定。

## 自审结论

- 没有把AI自述的内部抓取频率当作事实；
- 没有把回答正文URL当作消费端可见引用；
- 没有把提名和引用混入同一来源角色；
- 没有把API结果标记为消费端产品结果；
- 没有启用真实Provider、豆包生产采集、GEO连接或数据库迁移；
- 没有选择未批准的排行榜、时间窗和跨问题聚合语义；
- 没有初始化Git仓库。

## 最终验证

- `pnpm verify`通过；
- 类型检查通过，299项测试全部通过；
- 架构边界检查通过；
- 消费端观察不可执行OpenAPI草案与路由常量、Zod契约无漂移；
- 全项目格式检查通过；
- 100份Markdown无断链；
- 批准基线SHA-256清单无漂移；
- `.git`不存在，符合由项目所有者后续创建仓库的批准约定。
