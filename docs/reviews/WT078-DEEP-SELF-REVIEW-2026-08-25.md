# WT-078 深度自审

> 日期：2026-08-25  
> 结论：修复、全量验证、服务重建和浏览器实机验收通过

## 自审范围

- 新问题集保存后的运行表单选择；
- 运行创建前后的问题集、题数、采样次数、任务数和状态表达；
- 采集页当前运行与历史运行的区分；
- 既有运行、任务、证据、指标和数据库边界。

## 发现与处理

| 编号      | 风险                               | 处理                                       | 结果   |
| --------- | ---------------------------------- | ------------------------------------------ | ------ |
| SR-078-01 | 新问题集保存后仍保留旧问题集选择   | 使用服务端响应的问题集ID覆盖运行表单选择   | 已处理 |
| SR-078-02 | 创建运行前无法确认实际绑定内容     | 展示问题集、题数、采样次数和计划任务数     | 已处理 |
| SR-078-03 | 历史已完成运行被误认为新运行秒完成 | 运行选项增加任务数、状态、创建时间和短ID   | 已处理 |
| SR-078-04 | 切换运行后误以为其他行业数据消失   | 增加当前运行摘要并明确历史数据未覆盖       | 已处理 |
| SR-078-05 | 修复时自动清理误建运行导致数据损失 | 不删除、不改写任何既有运行或任务           | 已处理 |
| SR-078-06 | 为UI问题引入新API或迁移            | 只修改前端展示和静态测试，不新增依赖或迁移 | 已处理 |

## 验证结果

- 前端脚本语法检查：通过；
- 静态页面和脚本针对性测试：5项通过；
- 全量测试：400项通过，0项失败；
- TypeScript、分层边界、独立部署、OpenAPI和格式检查：通过；
- 本机服务：数据库、对象存储、liveness和readiness正常；
- 浏览器创建摘要：选择“广州装修推荐”后正确显示12项计划任务；
- 浏览器运行切换：24项待开始运行与16项已完成运行的摘要、列表和按钮同步更新；
- 实机验收没有点击创建、领取、确认或删除，不产生额外业务数据。

## 残余边界

当前误建的`be0a5508`运行仍保留为24项待领取任务。本次没有获批运行级删除功能，因此不自动删除；用户可以忽略该运行，后续如需要删除单次运行应单独设计可恢复性和证据清理边界。
