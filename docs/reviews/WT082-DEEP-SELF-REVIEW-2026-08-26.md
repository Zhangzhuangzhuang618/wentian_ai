# WT-082 深度自审

> 结论：主链路真实烟测通过；来源面板缺口由WT-083修复  
> 日期：2026-08-26  
> 对应任务：[`WT-082`](../tasks/WT-082-QIANWEN-WEB-SURFACE.md)

## Surface与数据隔离

- `qianwen_web`是独立受控Surface，没有复用或改写`doubao_web`身份；
- 创建运行、任务领取、自动化预检、扩展当前页面和证据上传均校验Surface一致；
- 千问草稿固定使用千问产品标签与官网Origin，跨平台草稿失败关闭；
- 既有豆包运行、任务、证据和排名未迁移、未重写。

## 采集与自动化边界

- 扩展只在千问官网根路径和批准的会话路径工作；
- 只采集用户当前可见回答区域内的正文、HTTP(S)链接和截图；
- 不读取Cookie、Web Storage、账号凭证、隐藏接口或网络响应；
- 自动化仍默认关闭，千问使用独立环境变量和独立预检门禁；
- 本机小规模测试配置为`synthetic`，生产示例配置仍默认为无授权并失败关闭；
- 自动提交结果仍进入人工复核，未确认内容不进入正式统计。

## 功能检查

- 前端可在豆包Web与千问Web之间选择，并在运行列表、采集摘要和任务信息中显示平台；
- 单题与整批接入码均携带任务Surface，千问任务返回独立页面签名；
- 扩展可识别千问官网、执行新对话、输入、发送、等待回答稳定和提交草稿；
- 首版不解析千问私有来源面板，只统计回答区域内实际可见的正式链接；
- 页面结构、发送状态、回答稳定状态或新对话入口无法确认时暂停，不提交不完整证据。

## 部署检查

- `0008_qianwen_web_surface.sql`已应用，数据库中的`qianwen_web`状态为`active`；
- 首次容器重建发现Dockerfile遗漏千问适配器清单，API因缺少工作区链接重启；
- Dockerfile已补齐千问适配器清单并重新运行完整门禁；
- 修复后容器重建成功，存活检查正常，就绪检查中数据库和对象存储均为`ready`；
- 部署后的首页资源包含`qianwen_web`和“千问 Web”选项。

## 验证证据

- `pnpm verify`：通过；
- 自动化测试：411项通过；
- TypeScript类型检查：通过；
- 源码分层边界：通过；
- 独立部署基础检查：通过，共8个连续迁移；
- OpenAPI与严格契约一致性：通过；
- 格式检查：通过；
- `GET /health/live`：正常；
- `GET /health/ready`：数据库与对象存储均为`ready`。

## 真实页面烟测补充

- 已在用户登录态完成新对话、提问、等待稳定回答、截图、提交复核和问天自动刷新；
- 烟测回答显示“6篇来源”，但首版未读取面板链接，问天预览为零信源；
- 该结果不能计作信源探测验收通过，已建立[`WT-083`](../tasks/WT-083-QIANWEN-VISIBLE-REFERENCE-PANEL.md)升级可见来源面板驱动；
- 千问页面后续若调整DOM，扩展会按失败关闭策略暂停，需要升级页面签名；
- WT-083真实烟测还需验证正式链接提取、预览和统计。

## 结论

WT-082主链路可用，数据归属、跨平台隔离和复核门禁未发现回归；正式来源提取不在WT-082完成结论内，以WT-083验收结果为准。
