# WT-080 深度自审

> 结论：通过  
> 日期：2026-08-26  
> 对应任务：[`WT-080`](../tasks/WT-080-QUESTION-GROUPED-CAPTURE-VIEW.md)

## 事实核对

- 截图中的重复问题来自同一运行配置的多次采样，不是中断续接创建了新运行；
- 整批续接继续领取原运行中状态为等待领取或采集中的记录，不新增样本槽位；
- 本次只改变前端分组和用户可见术语，既有运行、任务、证据与排名数据均未迁移或重写。

## 实现自审

- 分组键固定使用服务端返回的 `query_snapshot_item_id`，没有按易变的问题文本猜测归属；
- 组内按 `sample_index` 排序，保留每次采样的ID、状态和既有操作；
- 待采集、采集中或待复核的问题默认展开，全部结束的问题默认收起；
- 预览仍绑定原采样ID并请求原预览接口，不会把不同采样的回答混在一起；
- 运行摘要同时显示问题数、每题采样次数和总采样次数；
- 领取、整批接入、一键确认、运行创建和报告数量文案已改为“采样”，避免把样本数误读为独立实验数。

## 风险检查

- 自动同步仅在任务状态或版本变化时重绘列表；发生变化时展开状态按当前可操作性重新计算，可能收起刚完成的问题，但不影响数据或操作；
- 聚合只发生在浏览器内，服务端任务仍是执行和审计的最小单元；
- 无数据库迁移、无API字段变化、无新依赖，回滚只需恢复前端静态资源；
- 小屏下问题摘要和采样操作改为分行布局，避免操作按钮挤出卡片。

## 验证证据

- `pnpm verify`：通过；
- 自动化测试：405项通过；
- 源码边界：通过；
- 独立部署检查：通过，仍为7个迁移；
- OpenAPI一致性：通过；
- 格式检查：通过；
- `GET /health/live`：正常；
- `GET /health/ready`：数据库与对象存储均为 `ready`；
- 部署后的前端资源包含分组函数与新增样式，页面无控制台错误。

## 结论

实现符合 `DEC-WT080-001`。没有发现数据归属、证据隔离、统计口径或续接逻辑回归，可交付。
