# WT-075 深度自审

> 日期：2026-08-25  
> 结论：代码与自动验证通过，用户实机验证待补

## 自审范围

- 预览接口的项目、运行、任务绑定与返回字段；
- 一键确认的选取范围、确认提示、版本校验和失败恢复；
- 自然回答排名与信源自述域名复核的语义边界；
- 无迁移、无新增后台作业的独立部署影响。

## 发现与处理

| 编号      | 风险                                           | 处理                                                               | 结果   |
| --------- | ---------------------------------------------- | ------------------------------------------------------------------ | ------ |
| SR-075-01 | 一键确认误处理其他运行或项目                   | 点击时只快照当前页面任务，并沿用绑定任务ID和版本的单项确认接口     | 已处理 |
| SR-075-02 | 用户未查看内容就误触不可撤销操作               | 提供逐项预览；批量操作前显示数量、统计影响和不可撤销二次确认       | 已处理 |
| SR-075-03 | 预览接口泄露隐藏页面数据或内部证据位置         | 严格响应契约只返回可见回答、可见引用、有限元数据和截图已保存状态   | 已处理 |
| SR-075-04 | 任务ID与运行ID不一致仍能读取预览               | 服务端同时校验项目权限、任务所属运行、状态、证据归属和证据完整性   | 已处理 |
| SR-075-05 | 批量过程中一个任务失败导致已确认项重复提交     | 顺序提交并报告成功数量；刷新后只重新选取仍为`needs_review`的剩余项 | 已处理 |
| SR-075-06 | 自动刷新与批量确认并发导致页面版本过期         | 批量执行期间暂停任务自动同步，完成或失败后统一刷新当前项目         | 已处理 |
| SR-075-07 | 信源自述任务被误认为完成全部复核               | 一键确认只确认采集证据，域名提取复核继续使用独立状态与独立操作     | 已处理 |
| SR-075-08 | 为几十项操作引入批量事务、队列或新表造成复杂化 | 复用既有接口和状态模型，不新增迁移或后台基础设施                   | 已处理 |

## 自动验证结果

- 采集预览严格契约、真实接口和页面静态检查：通过；
- 全量测试：396项通过，0项失败；
- TypeScript类型检查：通过；
- 分层边界检查：通过；
- 独立部署基础检查：通过，迁移仍为7个；
- 消费端OpenAPI一致性检查：通过；
- 全仓格式检查：通过。
- 本机API容器重建：通过；存活、数据库和对象存储就绪检查均通过；
- 服务端页面资源已包含采集预览和一键确认入口。

## 待补验证

1. 用户对现有24项待复核结果完成预览及一键确认实测。
