# WT-063 消费端证据保留期执行

> 状态：已完成  
> 前置任务：[`WT-062`](./WT-062-STANDALONE-ATTENDED-CONSUMER-WORKBENCH.md)  
> 前置决策：[`DEC-WT063-001`](../decisions/DEC-WT063-001-CONSUMER-EVIDENCE-RETENTION-ENFORCEMENT.md)

## 目标

实际执行24小时、30天和180天保留期，并保留可审计、可复算的最小正式信源事实。

## 实现范围

- `0004`保留期、清理审计和原始记录生命周期；
- `0005`截图哈希、回答哈希、引用标题和提名说明的历史回填最小化；
- PostgreSQL与S3协调清理服务；
- 过期任务和运行状态原子重算；
- 手动/计划任务入口；
- 原始记录清除后的排名与对照读取。

## 完成标准

- [x] 待复核包到期后对象、Artifact和媒体行被删除；
- [x] 已确认截图到期后对象及截图哈希被清除；
- [x] 180天后正文、可见引用原始字段、说明和内容哈希被清除；
- [x] 正式`cited`/`nominated`事件仍支持排名和对照；
- [x] 清理运行成功或失败均有审计记录；
- [x] PostgreSQL 16与S3真实演练通过。

## 完成记录

- 完成日期：2026-08-23；
- 真实演练同时验证待复核过期、领取后放弃、截图到期和原始回答到期；
- 过期后累计排名仍为`a.com=3, b.com=1, c.com=1`，提名排名仍为`a.com=2, c.com=2, d.com=2`，Top-10重合仍为`2/10`。
